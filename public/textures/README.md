The 3D globe uses `earth-placeholder.svg` as a lightweight local texture.

For a more realistic look, replace it with an equirectangular Earth texture and update `GlobeMap.jsx` if the filename changes.

Recommended replacement:
- `public/textures/earth-blue-marble.jpg`
- 2:1 aspect ratio, for example 4096 x 2048
